#include<stdio.h>
int main (){
int length, width;
printf("enter the value of length\n");
scanf("%d",&length);
printf("Enter the value of width\n");
scanf("%d",&width);
printf("The value of the rectangle area is %d",length*width);
return 0;
}
